# Training stack: everything needed for inference, plus the dataset pipeline.
#
# Dropped from earlier versions of this file: evaluate, tqdm and pyyaml, which
# nothing imported, and pandas, whose only use was a DataFrame detour in
# train_lr.py that the shared split preparation replaced. `datasets` still
# installs pandas as its own dependency; it is just no longer ours.

-r inference-cpu.txt

datasets==4.4.1

# transformers.Trainer requires accelerate when training on torch.
accelerate==1.11.0
