# Local development: the inference stack plus test and lint tooling.

-r inference-cpu.txt

pytest==9.1.1

# Test client for the FastAPI layer.
httpx==0.28.1

ruff==0.16.8
black==26.5.1

# Produces requirements/*.lock; see `make lock`.
uv==0.12.19
